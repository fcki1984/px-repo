desc = "禁止访问文件"
author = "fcki1984"
version = 1
ops = [
  #读取您共享存储空间文件  
  {name = READ_EXTERNAL_STORAGE, mode =  deny }
  #写入共享存储空间文件  
  {name = WRITE_EXTERNAL_STORAGE, mode = deny  }
  #管理共享存储空间所有文件  
  {name = MANAGE_EXTERNAL_STORAGE, mode =  ignore }
  #读取音频  
  {name = READ_MEDIA_AUDIO, mode =  deny }
  #保存音频  
  {name = WRITE_MEDIA_AUDIO, mode = deny  }
  #读取视频  
  {name = READ_MEDIA_VIDEO, mode =  deny }
  #保存视频  
  {name = WRITE_MEDIA_VIDEO, mode =  deny }
  #读取图片  
  {name = READ_MEDIA_IMAGES, mode =  deny }
  #保存图片  
  {name = WRITE_MEDIA_IMAGES, mode =  deny }
  #存储兼容(不启用隔离存储)  
  {name = LEGACY_STORAGE, mode =  deny  }
  #禁用隔离存储  
  {name = NO_ISOLATED_STORAGE, mode = deny  }
]
